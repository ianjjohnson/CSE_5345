# Need to:
  # gather app, dependencies, assets, runtime, etc. into single bundle
  # copy bundle to server
  # install, then start or upgrade

#Distillery:
  # Gather everything needed to run
    # app, dependencies, assets, runtime
  # Package into single bundle

#Add Distillery to the deps list for an app
#Setup release tree (mix release.init)
